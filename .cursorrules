# test-client-design-system — AI Coding Instructions
# Generated by Guing v0.0.8 on 2026-03-05T14:13:57.743Z

## Package
Always import from `test-client-design-system`:
```tsx
import { Button, Text } from 'test-client-design-system';
import 'test-client-design-system/src/design-tokens/tokens.css';
```

## Design Token Rules
- NEVER hardcode hex colors — use CSS variables: `var(--color-primary)`
- NEVER hardcode font sizes — use CSS variables: `var(--font-heading-1-size)`
- For TypeScript access: `import { designTokens } from 'test-client-design-system'`

## Available Components (5 total)

### TextField
  TextField component
  Import: `import { TextField } from 'test-client-design-system'`
  Props:
    - variant: 'default' | 'light' | 'dark' (optional) — The variant of the text field, affecting its background color.
    - size: 'small' | 'medium' | 'large' (optional) — The size of the text field.
    - disabled: boolean (optional) — If true, the text field will be disabled.
    - className: string (optional) — Additional class name for custom styling.

### Button
  Button component
  Import: `import { Button } from 'test-client-design-system'`
  Props:
    - variant: 'default' | 'flat' | 'stroked' | 'basic' (optional) — The variant of the button.
- 'default': Standard button with background color.
- 'flat': Button with no background color.
- 'stroked': Button with a border.
- 'basic': Simple text button.
    - disabled: boolean (optional) — If true, the button will be disabled.
    - className: string (optional) — Additional class name for custom styling.

### LightDarkModeToggle
  LightDarkModeToggle component
  Import: `import { LightDarkModeToggle } from 'test-client-design-system'`
  Props:
    - size: 'small' | 'medium' | 'large' (optional) — The size of the toggle button
    - variant: 'default' | 'outlined' | 'text' (optional) — The variant of the toggle button
    - initialState: 'light' | 'dark' (optional) — The initial state of the toggle button
    - className: string (optional) — Additional class name for styling

### MultiFunctionButton
  MultiFunctionButton component
  Import: `import { MultiFunctionButton } from 'test-client-design-system'`
  Props:
    - variant: 'default' | 'flat' | 'stroked' | 'basic' (optional) — The variant of the button.
    - size: 'small' | 'medium' | 'large' (optional) — The size of the button.
    - disabled: boolean (optional) — If true, the button will be disabled.
    - className: string (optional) — Additional class names for styling.

### IconButton
  IconButton component
  Import: `import { IconButton } from 'test-client-design-system'`
  Props:
    - icon: React.ReactNode (required) — The icon to display inside the button.
    - size: 'small' | 'medium' | 'large' (optional) — The size of the button.
'small' | 'medium' | 'large'
    - variant: 'default' | 'flat' | 'stroked' (optional) — The variant of the button.
'default' | 'flat' | 'stroked'
    - disabled: boolean (optional) — If true, the button will be disabled.
    - className: string (optional) — Additional class names to apply to the button.
    - onClick: () => void (optional) — Click event handler.

## Token Summary
- Colors: 28 tokens
- Typography: 24 tokens

## Component Conventions
- All components accept a `className` prop for overrides
- Use `forwardRef` for DOM element access
- Include JSDoc for all public props
