# test-client-design-system — AI Coding Instructions
# Generated by Guing v0.0.4 on 2026-03-05T13:09:30.046Z

## Package
Always import from `test-client-design-system`:
```tsx
import { Button, Text } from 'test-client-design-system';
import 'test-client-design-system/src/design-tokens/tokens.css';
```

## Design Token Rules
- NEVER hardcode hex colors — use CSS variables: `var(--color-primary)`
- NEVER hardcode font sizes — use CSS variables: `var(--font-heading-1-size)`
- For TypeScript access: `import { designTokens } from 'test-client-design-system'`

## Available Components (3 total)

### TextField
  TextField component
  Import: `import { TextField } from 'test-client-design-system'`
  Props:
    - variant: 'default' | 'dialog' | 'section' (optional) — Background color variant for the text field
    - size: 'small' | 'medium' | 'large' (optional) — Size of the text field
    - disabled: boolean (optional) — Whether the text field is disabled
    - className: string (optional) — Additional class name for custom styling

### Button
  Button component
  Import: `import { Button } from 'test-client-design-system'`
  Props:
    - variant: 'default' | 'flat' | 'stroked' | 'basic' (optional) — The variant of the button.
    - size: 'small' | 'medium' | 'large' (optional) — The size of the button.
    - pressed: boolean (optional) — If true, the button will be in a pressed state.
    - disabled: boolean (optional) — If true, the button will be disabled.
    - className: string (optional) — Additional class name for custom styling.

### MultiFunctionButton
  MultiFunctionButton component
  Import: `import { MultiFunctionButton } from 'test-client-design-system'`
  Props:
    - size: 'small' | 'medium' | 'large' (optional) — The size of the button
    - variant: 'default' | 'flat' | 'stroked' | 'basic' (optional) — The variant of the button
    - disabled: boolean (optional) — If true, the button will be in a disabled state
    - toggleMode: boolean (optional) — If true, the button will toggle between light and dark mode
    - icon: React.ReactNode (optional) — Optional icon to display within the button
    - className: string (optional) — Additional className for styling overrides
    - onClick: () => void (optional) — Click event handler

## Token Summary
- Colors: 28 tokens
- Typography: 24 tokens

## Component Conventions
- All components accept a `className` prop for overrides
- Use `forwardRef` for DOM element access
- Include JSDoc for all public props
